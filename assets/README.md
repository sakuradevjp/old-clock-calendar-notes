# Hero screenshot

Drop a clean screenshot here as `hero.png` (or `.jpg`).

**Requirements:**
- No personal/family photos (use a landscape, illustration, or empty board)
- No personal names in the schedule text (use generic items like "コーヒー買いに行く")
- Recommended size: 1920x1080 or larger, 16:9 aspect

If `hero.png` is missing, the page falls back to a "スクリーンショット準備中" placeholder.
